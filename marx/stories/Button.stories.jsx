import { Button } from '../components/Button';

const meta = {
  component: Button,
  parameters: {
    layout: 'centered',
  },
};

export default meta;

export const Base = {
  args: {
    label: 'Button',
  },
};
