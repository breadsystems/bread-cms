import { Button } from '../js/Button';

const meta = {
  component: Button,
  parameters: {
    layout: 'centered',
  },
};

export default meta;

export const Base = {
  args: {
    children: 'Button',
  },
};
